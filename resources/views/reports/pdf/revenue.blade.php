<!DOCTYPE html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><style>*{font-family:'DejaVu Sans',sans-serif} body{direction:rtl; font-size:11px} table{width:100%; border-collapse:collapse; margin-top:10px} th,td{border:1px solid #cbd5e1; padding:5px 6px; text-align:right} th{background:#f1f5f9} h1{text-align:center} .meta{text-align:center; color:#64748b}</style></head><body>
<h1>تقرير الإيرادات — {{ $month }}/{{ $year }}</h1><p class="meta">المحصل: {{ number_format($totalPaid,0) }} — المطلوب: {{ number_format($totalRequired,0) }} — المتبقي: {{ number_format($totalDue,0) }} — الخصومات: {{ number_format($totalDiscount,0) }}</p>
<table><thead><tr><th>الطالب</th><th>المجموعة</th><th>الصافي</th><th>المدفوع</th><th>المتبقي</th><th>الحالة</th></tr></thead><tbody>@foreach($payments as $p)<tr><td>{{ $p->student->name ?? '—' }}</td><td>{{ $p->group->name ?? '—' }}</td><td>{{ number_format($p->net_required,0) }}</td><td>{{ number_format($p->paid_amount,0) }}</td><td>{{ number_format($p->remaining,0) }}</td><td>{{ $p->payment_status_label }}</td></tr>@endforeach</tbody></table>
<p class="meta" style="margin-top:12px">{{ now('Africa/Cairo')->format('Y/m/d H:i') }}</p>
</body></html>
